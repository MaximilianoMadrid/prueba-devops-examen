{
  "family": "smartlogix-db-orders",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "${EXECUTION_ROLE_ARN}",
  "taskRoleArn": "${EXECUTION_ROLE_ARN}",
  "containerDefinitions": [
    {
      "name": "db-orders",
      "image": "postgres:16-alpine",
      "essential": true,
      "portMappings": [
        { "name": "db-orders", "containerPort": 5432, "protocol": "tcp" }
      ],
      "environment": [
        { "name": "POSTGRES_DB", "value": "orders_db" },
        { "name": "POSTGRES_USER", "value": "postgres" }
      ],
      "secrets": [
        { "name": "POSTGRES_PASSWORD", "valueFrom": "${DB_ORDERS_PASSWORD_ARN}" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/smartlogix/db-orders",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
